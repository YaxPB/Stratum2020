using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Flip : MonoBehaviour
{
    public MovePlayer mp;

    public void Flippy(float horizontal)
    {
        /*if (horizontal < 0 && !mp.facingRight || horizontal > 0 && mp.facingRight)
        {
            mp.facingRight = !mp.facingRight;
            //Debug.Log("flipping");
            transform.Rotate(0f, 180f, 0f);
        }*/
    }

}
